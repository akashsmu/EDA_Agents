[pytest]
pythonpath = src
testpaths = tests
python_files = test_*.py
addopts = 
    -v
    --tb=short
    --strict-markers
    --strict-config
    --cov=backend
    --cov-report=html
    --cov-report=term-missing